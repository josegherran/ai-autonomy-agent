# ATK-generated build artifacts
appPackage/build/

# Secret / user env files — never commit
env/.env.*.user
env/.env.local
env/.env.playground

# Runtime config written by atk deploy
.localConfigs

# Node (if any scripts are added later)
node_modules/
